name: scheduled-dummy-workflow

on:
  schedule:
    # Runs every 5 minutes
    - cron: "*/5 * * * *"

  # Optional: allows manual trigger from GitHub UI
  workflow_dispatch:

jobs:
  dummy-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print hello message
        run: echo "👋 Hello from scheduled GitHub Actions workflow!"

      - name: Print date and time
        run: date

      - name: Simulate small task
        run: |
          echo "Running a dummy task..."
          sleep 5
          echo "Dummy task completed ✅"
