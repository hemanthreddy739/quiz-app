{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "A sample quiz application with Node.js and Express",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "node build.js",
    "start:dist": "node dist/index.js",
    "test": "jest --detectOpenHandles --forceExit --json --outputFile=test-results/test-results.json",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --detectOpenHandles --forceExit",
    "coverage:report": "jest --coverage --coverageReporters=html --detectOpenHandles --forceExit && open coverage/index.html"
  },
  "keywords": [
    "quiz",
    "nodejs",
    "express",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "jest": "^29.5.0",
    "jest-junit": "^16.0.0",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  }
}
